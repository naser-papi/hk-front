import { BlogsFilterList } from "@/components/organism";

const BlogsList = () => {
    return (
        <section id={"blogs-list"} className={"template"}>
            <BlogsFilterList />
        </section>
    );
};

export default BlogsList;
