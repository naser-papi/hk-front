import { BlogsFilterList } from "@/components/organism";

const BlogsList = () => {
    return (
        <section id={"blogs-list"} className={"template bg-white"}>
            <BlogsFilterList />
        </section>
    );
};

export default BlogsList;
