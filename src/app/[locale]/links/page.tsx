import { Footer, LinksList } from "@/components/template";

const LinksPage = () => {
    return (
        <main className="page-default-container">
            <LinksList />
            <Footer />
        </main>
    );
};

export default LinksPage;
